FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src ./src
COPY data ./data

ENV PYTHONPATH=/app/src

EXPOSE 8081

CMD ["python", "-m", "rag_alerts.cli", "serve-http", "--host", "0.0.0.0", "--port", "8081"]
